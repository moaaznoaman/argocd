apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: argocd-secret-test
  namespace: "{{ .Values.envType }}" # default
spec:
  dataFrom:
    - find:
        conversionStrategy: Default
        decodingStrategy: None
        name:
          regexp: "{{ .Values.envType }}ArgocdClient.*"
  refreshInterval: "30s"
  secretStoreRef:
    name: argocd-backend
    kind: ClusterSecretStore
  target:
    creationPolicy: Owner
    name: argocd-secret-test
    template:
      data:
        oidc.azure.clientId: '{{ printf "{{ .%sArgocdClientId }}" .Values.envType }}'
        oidc.azure.clientSecret: '{{ printf "{{ .%sArgocdClientSecret }}" .Values.envType }}'
      engineVersion: v2
      mergePolicy: Replace
      type: Opaque